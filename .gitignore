# 환경변수 (절대 올리면 안 됨!)
.env
*.env

# Frontend
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env.local

# Backend
backend/build/
backend/.gradle/
backend/bin/
backend/*.jar
backend/src/main/resources/application-local.properties

# AI Server
ai-server/venv/
ai-server/.venv/
ai-server/**/__pycache__/
ai-server/**/*.pyc

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# DB 덤프/백업 — 실제 운영 데이터라 올리면 안 된다.
# 사용자 이메일과 bcrypt 비밀번호 해시가 그대로 들어 있다.
# 시드 스크립트(seed-*.sql)는 지어낸 데이터라 커밋 대상이다.
infra/*backup*.sql
infra/*dump*.sql
*.dump
